- name: play level varaibles
  hosts:  local
  connection: local
  vars:
    NAME: "varun"
    CITY: "siddipet"
  tasks:
    - name: print varaibles
      ansible.builtin.debug:
        msg: "{{ NAME }} is staying in {{ CITY}}"